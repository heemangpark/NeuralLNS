device: cuda:2
epochs: 100
num_batch: 64
num_data: 4096
seed: 42
wandb: True

model:
  aggr: max
  delta: 1
  dim: 64
  num_layers: 3
  readout: sum
  type: MPNN

optimizer:
  lr: 1e-4
  weight_decay: 1e-2